Exercise Icons

Usage: capture.js [options]

    -h --help       Show this message
    -f --file       get list of exercises to render from the given JSON file
    -u --upload     upload to s3. Auth is taken from env variables S3_KEY,
                    S3_SECRET and S3_BUCKET
    -m --manifest   Don't take screenshots, just create the manifest file

